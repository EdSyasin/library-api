<%- include('../components/layout-start') %>

<div class="container mt-5">
    <% if (success.bookAdded) { %>
    <div class="alert alert-success" role="alert">
        <%= success.bookAdded %>
    </div>
    <% } %>
    <% if (book.showCount) { %>
        Книга просмотрена <%= book.showCount %> раз
    <% } %>

    <%- include('../components/bookForm', { book: book }) %>

</div>

<%- include('../components/layout-end') %>
